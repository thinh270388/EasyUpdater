<Window x:Class="EasyUpdater.Core.Views.UpdateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyUpdater.Core.Views"
        mc:Ignorable="d"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Height="200" Width="400">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="{Binding Title}" 
                       FontSize="16" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,10"/>
            <ProgressBar Height="20" Minimum="0" Maximum="100" 
                         Value="{Binding Progress}" />
            <TextBlock Text="{Binding Status}" 
                       FontSize="12" Margin="0,10,0,0" TextAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>
